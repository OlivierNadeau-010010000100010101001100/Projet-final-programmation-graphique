<Page
    x:Class="ProjetFinal.PageSupprimerSeance"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjetFinal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <TextBlock x:Name="TestMessage" />
        <ScrollViewer>
            <ListView x:Name="LVTestSeance" Grid.Row="8" MaxHeight="500" BorderBrush="#CCCCCC" BorderThickness="1" Padding="10">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Key}" Width="200" />
                            <TextBlock Text="{Binding Value.Date}" Width="100" />
                            <TextBlock Text="{Binding Value.Heure}" Width="100" />
                            <TextBlock Text="{Binding Value.NbrPlaces}" Width="100" />
                            <TextBlock Text="{Binding Value.ActiviteIDFK}" Width="100" />
                            
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Page>
